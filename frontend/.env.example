# 백엔드 주소. 실기기(Expo Go)에서는 localhost 대신 PC의 LAN IP를 써야 함 (예: http://192.168.0.10:8080)
EXPO_PUBLIC_API_URL=http://localhost:8080
# true면 로그인 화면에 개발용 로그인(/api/dev/auth/token) 버튼 노출
EXPO_PUBLIC_DEV_LOGIN=true
